# Spec changelog

Append **newest first** under `## Log`. Reference ids (`FR-…`, `NFR-…`, `S…`).
See `docs/spec-schema.md`.

## Log

### YYYY-MM-DD — Initial baseline

- Seeded `prd.spec.yaml`, `spec-digest.md`, and this log from **spec-draft-prd**.
